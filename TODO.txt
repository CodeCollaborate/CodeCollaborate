TODOs for intergoprocdandling:
    immediate/required:
        TODO: update dbfs/databases.go with the documentation from the databasesImpl methods

    testing/required:
        TODO: testing... lots of testing

    normal/required:
        TODO: check if permission high enough on project before making these actions
            Project.RevokePermissions
            Project.GrantPermissions
            Project.Rename
            File.Create
            File.Rename
            File.Move
            File.Delete
            File.Change
            File.Pull
	    TODO: inform the client if they're not authenticated

	non-immediate/required:
	    TODO: implement user authentication in modules/datahandling/datahandler.go:53
	    TODO: verify changes in request for File.Change are valid changes
	    TODO: figure out how we want to do projectGetPermissionConstantsRequest

	optional:
		TODO: un-hardcode '10' as the owner constant in the MySQL ProjectLookup stored proc
	    TODO: close ws connection on malicious request


Decisions:
    TODO: decide if we want 1 publisher per connection (modules/handlers/wsmanager.go:66) or 1 publisher for the whole server (move that to runner)
    TODO: decide on change to MySQLProjectLookup stored proc
    TODO: decide on action at rabbit publish error: retry with count?


things to put on redis:
        permission constants
        online clients
        cursors (eventually, not in current feature-set)
        temporary storage of project permissions w/ decaying timer? (possibly, but not likely)
        subscriptions? (probably not)
